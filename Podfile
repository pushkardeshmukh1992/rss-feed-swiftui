# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RSSFeed' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RSSFeed
	pod 'XMLCoder', '~> 0.14.0'

  target 'RSSFeedTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
